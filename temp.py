m5 = build_measure(
            f.note(F1, self.q),
            rest(self.h),
            f.note(F1, self.q)
        )

        m6 = build_measure(
            f.note(F1, self.q),
            rest(self.h),
            f.note(F1, self.q)
        )

        m7 = build_measure(
            f.note(E1, self.q),
            rest(self.h),
            f.note(E1, self.q)
        )

        m8 = build_measure(
            f.note(E1, self.q),
            rest(self.h),
            f.note(E1, self.q)
        )